version: "2"
sql:
  - engine: "mysql"
    queries: "sql/queries"
    schema: "sql/schema.sql"
    gen:
      go:
        package: "store"
        out: "store"
        emit_json_tags: true
        emit_pointers_for_null_types: true
